name: Build Godot AAB (FIXED)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 📥 المشروع
      - uses: actions/checkout@v4

      # ☕ Java
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      # 🧰 أدوات
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y wget unzip

      # 🎮 تحميل Godot
      - name: Download Godot
        run: |
          wget https://github.com/godotengine/godot/releases/download/4.6.3-stable/Godot_v4.6.3-stable_linux.x86_64.zip
          unzip Godot_v4.6.3-stable_linux.x86_64.zip
          chmod +x Godot_v4.6.3-stable_linux.x86_64

      # 📦 تحميل Export Templates (🔥 FIX الأساسي)
      - name: Install Export Templates
        run: |
          mkdir -p ~/.local/share/godot/export_templates/4.6.3.stable

          wget https://github.com/godotengine/godot/releases/download/4.6.3-stable/Godot_v4.6.3-stable_export_templates.tpz

          unzip Godot_v4.6.3-stable_export_templates.tpz -d tmp_templates

          mv tmp_templates/templates/* ~/.local/share/godot/export_templates/4.6.3.stable/

      # 📌 عرض المشروع
      - name: Show project
        run: ls -R

      # 🏗️ Build AAB
      - name: Export Android AAB
        run: |
          ./Godot_v4.6.3-stable_linux.x86_64 --headless \
            --path . \
            --export-release "Android"

      # 🔍 العثور على AAB
      - name: Find AAB
        run: find . -name "*.aab" || true

      # 📦 رفع الملف
      - name: Upload AAB
        uses: actions/upload-artifact@v4
        with:
          name: godot-aab
          path: "**/*.aab"
